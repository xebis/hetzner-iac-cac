#cloud-config
---
users:
  - default
  - name: github
    gecos: GitHub
    primary_group: users
    groups: users, admin
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    shell: /bin/bash
    ssh_authorized_keys:
      - ${ssh_id_pub}
